# Rails
RAILS_ENV=development
SECRET_KEY_BASE=your-secret-key-here-generate-with-rails-secret

# Database (optional — defaults work for local Postgres)
# DATABASE_USERNAME=
# DATABASE_PASSWORD=
# DATABASE_HOST=localhost
